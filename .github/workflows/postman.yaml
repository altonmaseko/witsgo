name: Run Postman Tests

on:
  push:
    branches:
      - dev

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Start the server
        run: npm start &
        env:
          PORT: 3000
          CONNECTION_URI: mongodb+srv://lucian:lucian-witsgo@hexacluster.sjjuf.mongodb.net/
          GOOGLE_MAP_KEY: AIzaSyCEbj6_WGhOUTbbDAVNw3BhOMnQ44yyXOo
          GOOGLE_CLIENT_ID: 1
          GOOGLE_CLIENT_SECRET: 1
          SESSION_SECRET: temp

      - name: Install Newman
        run: npm install -g newman

      - name: Run Postman tests
        run: newman run "https://api.getpostman.com/collections/33683923-80216182-2ad9-4a3f-bbea-9cca9a8106af?apikey=PMAK-66e889ccb6c61c0001a29e1d-9ca34dfbd7be40c1aae35249ca202729ed"

      - name: Stop the server
        run: kill $(lsof -t -i:3000) || true
